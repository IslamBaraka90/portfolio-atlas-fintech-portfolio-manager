# Planned module

Construct configuration, clock, repository, provider, analytics, and broker dependencies here. No financial rule should be hidden in wiring.

Chapter 1 composition is implemented in ../app.ts; it selects memory storage and injects the clock and ID factory.
