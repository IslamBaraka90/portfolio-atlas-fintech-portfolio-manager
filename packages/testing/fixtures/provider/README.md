# Planned module

Only schema-shaped synthetic provider fixtures belong here by default. Real response caches stay in ignored .data/ until redistribution rights are established.

This directory currently documents ownership; it contains no runtime implementation.
